# Variáveis do banco usadas pelo docker-compose.yml.
# Copie este arquivo para .env e ajuste se quiser sobrescrever os padrões.
# Os valores abaixo batem com backend/src/main/resources/application.properties.example.

POSTGRES_DB=trackfibra
POSTGRES_USER=trackfibra
POSTGRES_PASSWORD=trackfibra
POSTGRES_PORT=5432
